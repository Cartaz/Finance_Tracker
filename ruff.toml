# Reconciliation parses bank statement calendar dates, not instants.
# datetime.strptime(...).date() intentionally discards time semantics, so the
# two inline DTZ007 exceptions in that parser are deliberate and reviewable.
[lint]
extend-select = ["DTZ007"]
